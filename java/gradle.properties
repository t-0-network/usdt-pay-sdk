version=0.1.3
group=network.t-0

# Pinned t-0 provider SDK — supplies crypto (Signer, SignatureVerifier) and the
# signed transport (ProviderServer, BlockingNetworkClient). Not a floating range:
# the transport contract is the one thing a partner must not get silently bumped.
providerSdkVersion=1.1.28
