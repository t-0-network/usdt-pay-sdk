// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tzero/v1/pay/lp/lp.proto

// The LP ↔ t-0 edge:
// LpService is hosted by t-0 and called by the LP;
// LpCallbackService is implemented by the LP and called by t-0.

package lpconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	lp "github.com/t-0-network/usdt-pay-sdk/go/sdk/gen/tzero/v1/pay/lp"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LpServiceName is the fully-qualified name of the LpService service.
	LpServiceName = "tzero.v1.pay.lp.LpService"
	// LpCallbackServiceName is the fully-qualified name of the LpCallbackService service.
	LpCallbackServiceName = "tzero.v1.pay.lp.LpCallbackService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LpServicePublishQuoteProcedure is the fully-qualified name of the LpService's PublishQuote RPC.
	LpServicePublishQuoteProcedure = "/tzero.v1.pay.lp.LpService/PublishQuote"
	// LpServiceFiatSettlementSentProcedure is the fully-qualified name of the LpService's
	// FiatSettlementSent RPC.
	LpServiceFiatSettlementSentProcedure = "/tzero.v1.pay.lp.LpService/FiatSettlementSent"
	// LpCallbackServiceExecuteQuoteProcedure is the fully-qualified name of the LpCallbackService's
	// ExecuteQuote RPC.
	LpCallbackServiceExecuteQuoteProcedure = "/tzero.v1.pay.lp.LpCallbackService/ExecuteQuote"
)

// LpServiceClient is a client for the tzero.v1.pay.lp.LpService service.
type LpServiceClient interface {
	// * Pushes immutable standing quotes into t-0's Order Book, at most one per currency per call; each is multi-consumable while it stands.
	PublishQuote(context.Context, *connect.Request[lp.PublishQuoteRequest]) (*connect.Response[lp.PublishQuoteResponse], error)
	// *
	// Reports a fiat bank-rails settlement the LP made on its own initiative against accepted quote executions.
	// An execution still awaiting its durable result returns FAILED_PRECONDITION; retry the same request.
	FiatSettlementSent(context.Context, *connect.Request[lp.FiatSettlementSentRequest]) (*connect.Response[lp.FiatSettlementSentResponse], error)
}

// NewLpServiceClient constructs a client for the tzero.v1.pay.lp.LpService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLpServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LpServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	lpServiceMethods := lp.File_tzero_v1_pay_lp_lp_proto.Services().ByName("LpService").Methods()
	return &lpServiceClient{
		publishQuote: connect.NewClient[lp.PublishQuoteRequest, lp.PublishQuoteResponse](
			httpClient,
			baseURL+LpServicePublishQuoteProcedure,
			connect.WithSchema(lpServiceMethods.ByName("PublishQuote")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
		fiatSettlementSent: connect.NewClient[lp.FiatSettlementSentRequest, lp.FiatSettlementSentResponse](
			httpClient,
			baseURL+LpServiceFiatSettlementSentProcedure,
			connect.WithSchema(lpServiceMethods.ByName("FiatSettlementSent")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
	}
}

// lpServiceClient implements LpServiceClient.
type lpServiceClient struct {
	publishQuote       *connect.Client[lp.PublishQuoteRequest, lp.PublishQuoteResponse]
	fiatSettlementSent *connect.Client[lp.FiatSettlementSentRequest, lp.FiatSettlementSentResponse]
}

// PublishQuote calls tzero.v1.pay.lp.LpService.PublishQuote.
func (c *lpServiceClient) PublishQuote(ctx context.Context, req *connect.Request[lp.PublishQuoteRequest]) (*connect.Response[lp.PublishQuoteResponse], error) {
	return c.publishQuote.CallUnary(ctx, req)
}

// FiatSettlementSent calls tzero.v1.pay.lp.LpService.FiatSettlementSent.
func (c *lpServiceClient) FiatSettlementSent(ctx context.Context, req *connect.Request[lp.FiatSettlementSentRequest]) (*connect.Response[lp.FiatSettlementSentResponse], error) {
	return c.fiatSettlementSent.CallUnary(ctx, req)
}

// LpServiceHandler is an implementation of the tzero.v1.pay.lp.LpService service.
type LpServiceHandler interface {
	// * Pushes immutable standing quotes into t-0's Order Book, at most one per currency per call; each is multi-consumable while it stands.
	PublishQuote(context.Context, *connect.Request[lp.PublishQuoteRequest]) (*connect.Response[lp.PublishQuoteResponse], error)
	// *
	// Reports a fiat bank-rails settlement the LP made on its own initiative against accepted quote executions.
	// An execution still awaiting its durable result returns FAILED_PRECONDITION; retry the same request.
	FiatSettlementSent(context.Context, *connect.Request[lp.FiatSettlementSentRequest]) (*connect.Response[lp.FiatSettlementSentResponse], error)
}

// NewLpServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLpServiceHandler(svc LpServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	lpServiceMethods := lp.File_tzero_v1_pay_lp_lp_proto.Services().ByName("LpService").Methods()
	lpServicePublishQuoteHandler := connect.NewUnaryHandler(
		LpServicePublishQuoteProcedure,
		svc.PublishQuote,
		connect.WithSchema(lpServiceMethods.ByName("PublishQuote")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	lpServiceFiatSettlementSentHandler := connect.NewUnaryHandler(
		LpServiceFiatSettlementSentProcedure,
		svc.FiatSettlementSent,
		connect.WithSchema(lpServiceMethods.ByName("FiatSettlementSent")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.pay.lp.LpService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LpServicePublishQuoteProcedure:
			lpServicePublishQuoteHandler.ServeHTTP(w, r)
		case LpServiceFiatSettlementSentProcedure:
			lpServiceFiatSettlementSentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLpServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLpServiceHandler struct{}

func (UnimplementedLpServiceHandler) PublishQuote(context.Context, *connect.Request[lp.PublishQuoteRequest]) (*connect.Response[lp.PublishQuoteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.pay.lp.LpService.PublishQuote is not implemented"))
}

func (UnimplementedLpServiceHandler) FiatSettlementSent(context.Context, *connect.Request[lp.FiatSettlementSentRequest]) (*connect.Response[lp.FiatSettlementSentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.pay.lp.LpService.FiatSettlementSent is not implemented"))
}

// LpCallbackServiceClient is a client for the tzero.v1.pay.lp.LpCallbackService service.
type LpCallbackServiceClient interface {
	// * Requests a durable Accepted or Rejected result; only Accepted creates the LP's firm per-payment obligation.
	ExecuteQuote(context.Context, *connect.Request[lp.ExecuteQuoteRequest]) (*connect.Response[lp.ExecuteQuoteResponse], error)
}

// NewLpCallbackServiceClient constructs a client for the tzero.v1.pay.lp.LpCallbackService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLpCallbackServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LpCallbackServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	lpCallbackServiceMethods := lp.File_tzero_v1_pay_lp_lp_proto.Services().ByName("LpCallbackService").Methods()
	return &lpCallbackServiceClient{
		executeQuote: connect.NewClient[lp.ExecuteQuoteRequest, lp.ExecuteQuoteResponse](
			httpClient,
			baseURL+LpCallbackServiceExecuteQuoteProcedure,
			connect.WithSchema(lpCallbackServiceMethods.ByName("ExecuteQuote")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
	}
}

// lpCallbackServiceClient implements LpCallbackServiceClient.
type lpCallbackServiceClient struct {
	executeQuote *connect.Client[lp.ExecuteQuoteRequest, lp.ExecuteQuoteResponse]
}

// ExecuteQuote calls tzero.v1.pay.lp.LpCallbackService.ExecuteQuote.
func (c *lpCallbackServiceClient) ExecuteQuote(ctx context.Context, req *connect.Request[lp.ExecuteQuoteRequest]) (*connect.Response[lp.ExecuteQuoteResponse], error) {
	return c.executeQuote.CallUnary(ctx, req)
}

// LpCallbackServiceHandler is an implementation of the tzero.v1.pay.lp.LpCallbackService service.
type LpCallbackServiceHandler interface {
	// * Requests a durable Accepted or Rejected result; only Accepted creates the LP's firm per-payment obligation.
	ExecuteQuote(context.Context, *connect.Request[lp.ExecuteQuoteRequest]) (*connect.Response[lp.ExecuteQuoteResponse], error)
}

// NewLpCallbackServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLpCallbackServiceHandler(svc LpCallbackServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	lpCallbackServiceMethods := lp.File_tzero_v1_pay_lp_lp_proto.Services().ByName("LpCallbackService").Methods()
	lpCallbackServiceExecuteQuoteHandler := connect.NewUnaryHandler(
		LpCallbackServiceExecuteQuoteProcedure,
		svc.ExecuteQuote,
		connect.WithSchema(lpCallbackServiceMethods.ByName("ExecuteQuote")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.pay.lp.LpCallbackService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LpCallbackServiceExecuteQuoteProcedure:
			lpCallbackServiceExecuteQuoteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLpCallbackServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLpCallbackServiceHandler struct{}

func (UnimplementedLpCallbackServiceHandler) ExecuteQuote(context.Context, *connect.Request[lp.ExecuteQuoteRequest]) (*connect.Response[lp.ExecuteQuoteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.pay.lp.LpCallbackService.ExecuteQuote is not implemented"))
}
