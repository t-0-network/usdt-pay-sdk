{
  "name": "usdt-pay-sdk-node",
  "private": true,
  "description": "npm workspace holding the pay SDK and the per-role starters",
  "workspaces": [
    "sdk",
    "starter/issuer",
    "starter/lp"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
