# The build context is the repository root, so keep host build output — and any
# .env a starter picked up — out of the context and the build cache.
.git
.gitignore
**/build/
**/.gradle/
**/node_modules/
**/dist/
node/sdk/lib/
**/.env
.idea
.DS_Store
